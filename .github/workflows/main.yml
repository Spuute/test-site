# name: Deploy to GitHub Pages

# on:
#   push:
#     branches: [ main ]
    
# jobs:
#   deploy-to-github-pages:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v2
#     - name: Setup .NET Core SDK
#       uses: actions/setup-dotnet@v1.9.0
#       with:
#         dotnet-version: 6.0.x
#     - name: Publish Blazor project to the release-folder
#       run: dotnet publish test-site.csproj -c Release -o release --nologo
#     - name: Committ wwwroot to Github Pages
#       uses: JamesIves/github-pages-deploy-action@v4.2.5
#       with:
#         GITHUB_TOKEN: ${{ secrets.TOKEN }}
#         BRANCH: gh-pages
#         FOLDER: release/wwwroot
#     - name: Change base-tag in index.html from / to test-site
#       run: sed -i 's/<base href="\/" \/>/<base href="\/test-site\/" \/>/g' release/wwwroot/index.html
#     # add .nojekyll file to tell GitHub pages to not treat this as a Jekyll project. (Allow files and folders starting with an underscore)
#     - name: Add .nojekyll file
#       run: touch release/wwwroot/.nojekyll
#     - name: Publish
#       run: |
#         cp wwwroot/index.html wwwroot/404.html
#         dotnet publish test-site.csproj --configuration Release
name: 'Publish application'
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        # Checkout the code
        - uses: actions/checkout@v2

        # Install .NET Core SDK
        - name: Setup .NET Core
          uses: actions/setup-dotnet@v1.9.0
          with:
            dotnet-version: 6.0.x

        # Run tests
        - name: Test
          run: dotnet test

        # Generate the website
        - name: Publish
          run: dotnet publish test-site.csproj --configuration Release

        # Publish the website
        - name: GitHub Pages action
          if: ${{ github.ref == 'refs/heads/master' }} # Publish only when the push is on master
          uses: peaceiris/actions-gh-pages@v3.6.1
          with:
            github_token: ${{ secrets.TOKEN }}
            publish_branch: gh-pages
            publish_dir: bin/Release/netstandard2.1/publish/wwwroot
            allow_empty_commit: false
            keep_files: false
            force_orphan: true
            # TODO uncomment the following line if you have a custom domain name
            # cname: demo.meziantou.net
        - name: Publish
          run: |
            cp wwwroot/index.html wwwroot/404.html
            dotnet publish test-site.csproj --configuration Release